const HeaderTitle = () => {
    return (
        <input 
            className="text-center rounded-full w-auto bg-transparent text-gray-400" 
            defaultValue={"New Project"}>
        </input>
    );
};

export default HeaderTitle;